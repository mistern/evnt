{% extends 'frontend/base.html.twig' %}

{% block body %}
    <nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="{{ path('homepage') }}">⚫ evnt</a>
            <button class="navbar-toggler" type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#navbar"
                    aria-controls="navbar"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbar">
                <ul class="navbar-nav navbar-nav-scroll me-auto mb-2 mb-lg-0" style="--bs-scroll-height: 50vh;">
                    <li class="nav-item">
                        <a class="nav-link{{ app.request.attributes.get('_route') == 'events.listing' ? ' active' }}"
                           href="{{ path('events.listing') }}">Events</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container">
        {% block contents %}{% endblock %}
    </div>
{% endblock %}
